# Env — Phase 3

See `.AiControl/root/env.md` for full toolchain setup.

Phase-specific:
- Install: `react-markdown`, `remark-gfm`, `rehype-raw`, `rehype-highlight`
- Copy shadcn-chatbot-kit components (npx shadcn add or manual copy)
- LLM API keys go in config.toml (not committed to git)
