# Env — Phase 5

See `.AiControl/root/env.md` for full toolchain setup.

Phase-specific: ripgrep must be bundled as a Tauri resource. The `grep` crate is used programmatically — no CLI binary dependency.
