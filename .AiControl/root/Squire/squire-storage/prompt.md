# Prompt

Use this node to implement Squire's storage layer.

Deliverables:
- LanceDB schema covering structured and raw partitions (no SQLite-only stopgap).
- Triplet store and vector search path shipped with production parity in v1.
- Storage interface kept abstracted so test doubles and future backend flexibility remain possible.

Reference: `../planning/decisions.md` Q4 for the resolved rule and scope implications.
