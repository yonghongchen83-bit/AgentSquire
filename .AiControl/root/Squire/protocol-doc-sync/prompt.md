# Prompt

Use this node to bring `context_squire_spec_v2.md` in line with the locked runtime contract.

Deliverables:
- Document the system-prompt superset as the canonical v1 contract: `resource_type` supports `tool_skill`, `concept`, `referential` in addition to base types; `max_results` behavior documented.
- Note compatibility rule: existing base types remain supported; superset types are additive and non-breaking.

Reference: `../planning/decisions.md` Q1 "Follow-up action".
