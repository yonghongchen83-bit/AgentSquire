# State

## Timeline
- 2026-07-02: Node created, split out of root/Squire/planning as implementation step 4 of the incremental delivery plan.
- 2026-07-02: Resumed. Repointed `.AiControl/.current` to this node (was stale at `squire-adapter`). Read `squire-adapter/decisions.md` (`SquireStore` trait contract, Q5 tool exposure, the specific `SquireInvokeTool` redirect pointer) and `planning/decisions.md` Q4. Verified baseline: `cargo build` clean, `cargo test --lib` 117/117 passing — matches handoff.md exactly, no drift.
- 2026-07-02: Confirmed unblocked (squire-adapter completed, `SquireStore` trait exists in `squire.rs`). Populated `todo.json` (was empty scaffolding) with ss-1..ss-8 covering: schema design, triplet+vector search implementation, storage abstraction, dependency addition, AppState wiring, invoke-tool redirect, unit tests, final verification.
- 2026-07-02: Spiked adding `lancedb = "0.14"` to `Cargo.toml` to validate feasibility before committing to the design. Discovered `lance-encoding`'s build script requires system `protoc` (protobuf compiler) — not present on this machine by default. Installed via `winget install Google.Protobuf` and built with `PROTOC` env var pointed at the extracted binary; clean build in ~7 min cold (dependency compilation, not our code), `cargo test --lib` still 117/117 with the dependency present but unused. Decision: proceed with real `lancedb` crate per Q4 ("no SQLite-only stopgap"), but document the `protoc`/`PROTOC` dev-machine requirement in `env.md` as a new durable fact — this is a new build prerequisite for anyone building this repo from a clean machine.
- 2026-07-02: Implemented `src-tauri/src/storage/squire_lancedb.rs`: `LanceDbSquireStore` implementing `SquireStore` against three LanceDB tables (`squire_tokens` = structured partition with a fixed-size-list embedding column for vector search, `squire_relationships` = triplet store, `squire_turns` = per-session turn counter). Vector search backed by a deterministic hash-based bag-of-words embedding (`embed_text`, 64-dim) since no embedding-model provider is wired into this codebase yet — documented as a scoping decision below, swappable later without touching the `SquireStore` trait or callers. See `decisions.md`.
- 2026-07-02: Wired `AppState.squire_store` to `LanceDbSquireStore` in `setup_cmd.rs`, opening/creating the LanceDB dir at `<app_config_dir>/squire_lancedb`. Redirected `SquireInvokeTool`'s lookup: it now tries `store.token_detail(token_id)` first to resolve a real ingested tool-token to an MCP-endpoint-shaped detail, falling back to `tool_registry.get(token_id)` (today's MCP-proxy-by-name reality) so existing MCP/local tool invocation is unaffected — see decisions.md for exact precedence rationale.
- 2026-07-02: Added unit tests for `LanceDbSquireStore` mirroring `InMemorySquireStore`'s coverage (token/relationship/preserve-list roundtrip, turn counter, explore_memory type+query filtering and max_results truncation, vector-similarity ranking sanity check). Ran full `cargo build` + `cargo test --lib` with `PROTOC` set — see closing summary for final counts.

- 2026-07-02: Full verification: `cargo build` (lib+bin, `PROTOC` set) clean, zero warnings. `cargo test --lib` (both with and without `PROTOC` set, confirming it's only needed for cold builds): 126/126 passing (117 baseline + 8 new `storage::squire_lancedb::tests` + 1 new `agent::squire::tests::invoke_tool_falls_back_to_store_token_detail_when_not_in_registry`). `npx tsc --noEmit -p tsconfig.app.json`: identical pre-existing errors to before this session (tools-panel.tsx `disabledTools`/icon `title` prop issues), nothing new — Rust-only session.

## Closure summary

All three original deliverables (LanceDB schema, triplet store + vector search, storage abstraction) are implemented in `src-tauri/src/storage/squire_lancedb.rs` as `LanceDbSquireStore`, implementing the existing `SquireStore` trait from `agent/squire.rs` with zero trait changes. Wired into `AppState.squire_store` via `setup_cmd.rs` (LanceDB dir at `<app_config_dir>/squire_lancedb`), replacing `InMemorySquireStore` as the production store; `InMemorySquireStore` remains available for fast unit tests elsewhere. Addressed the specific `SquireInvokeTool` redirect pointer from squire-adapter/decisions.md as a fallback-after-registry lookup (see decisions.md for why a hard cutover isn't safe/possible yet). One new dependency risk (`lancedb` requiring system `protoc` for cold builds) was discovered, resolved (`winget install Google.Protobuf`), verified end-to-end, and documented in `env.md` as a new repo-wide build prerequisite.

Scoped out and explicitly flagged as a follow-up (ss-9 in todo.json, not silently absorbed): real tool-token ingestion (a write path turning MCP/local tool discovery into persisted, invocable `SquireStore` rows with endpoint metadata). Without it, `SquireInvokeTool`'s new store-fallback path is exercised and correct but will only ever hit "no invocable endpoint bound yet" in practice today, since nothing currently writes tool tokens into the store. This does not block marking this node's stated scope (storage layer) complete — the storage layer itself is fully real and functional (tokens/relationships/preserve-lists/turns all round-trip through actual LanceDB tables, vector search performs genuine cosine-similarity ranking).

All todo.json items ss-1 through ss-8 are DONE. ss-9 is the one OPEN item, explicitly out of this node's scope per env.md's non-goals, left for whichever future node picks up full tool-token ingestion (likely intersects with rejection-ux or a new node, not squire-storage).

## Next Actions
- Node scope complete. Repoint `.AiControl/.current` to `root/Squire/rejection-ux` (the other unblocked sibling) or `root/Squire/protocol-doc-sync` for the next session — see updated `../handoff.md`.
- ss-9 (tool-token ingestion) is a real gap for Q5's full vision but not yet assigned to any node; flag when scoping the next piece of Squire work.
