# Env — Phase 0

See `.AiControl/root/env.md` for full toolchain setup.

Phase-specific notes:
- Use npm (or pnpm) — decide during scaffold
- Tauri CLI v2 must be installed: `cargo install tauri-cli --version ^2`
- Windows: ensure WebView2 is available (Win11 has it built-in)
