# Env — Phase 2

See `.AiControl/root/env.md` for full toolchain setup.

Phase-specific:
- Install npm packages: `@monaco-editor/react`, `@xterm/xterm`, `@xterm/addon-fit`, `momoi-explorer`, `momoi-explorer/react`
- Copy shadcn resizable components if not already added
- Tauri shell plugin must be configured in Cargo.toml + capabilities
