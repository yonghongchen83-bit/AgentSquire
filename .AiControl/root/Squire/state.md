# State

## Child Nodes

`planning` (completed 2026-07-02) produced a locked architecture for the ContextManagerAdapter (see `planning/implementation-readiness.md`). Implementation is organized into sibling nodes, each scoped to one technical context, in dependency order:

1. `adapter-core` — adapter trait + insertion seam + LegacyContextAdapter extraction (✅ completed 2026-07-02)
2. `session-mode` — context_mode persistence + immutability guard + routing (✅ completed 2026-07-02)
3. `squire-adapter` — SquireContextAdapter + strict tool surface + validation gates (✅ completed 2026-07-02 — see its decisions.md for the `SquireStore` trait contract and two follow-up gaps flagged as sa-4/sa-5)
4. `squire-storage` — LanceDB storage stack (✅ completed 2026-07-02 — real `LanceDbSquireStore` implements `SquireStore` against LanceDB; see its decisions.md for the `protoc` build-prerequisite finding, the deterministic-embedding placeholder decision, and the `SquireInvokeTool` fallback-redirect rationale. One follow-up flagged, not blocking: `ss-9` — real tool-token ingestion, not yet assigned to a node)
5. `rejection-ux` — compliance-failure UX + diagnostics + preserve-list lifecycle (✅ completed 2026-07-02 — real Q6 UX: `TurnOutcome::Failed` now persists a visible chat message plus a structured `squire_compliance_failures` LanceDB record instead of the interim transient diagnostic; real Q7 lifecycle: `SquireStore::clear_all_preserve_lists()` wipes carryover once at app startup. See its decisions.md for why sa-4/sa-5 were considered and deliberately deferred rather than absorbed.)
6. `protocol-doc-sync` — spec doc sync (✅ completed 2026-07-02 — both `context_squire_spec_v2.md` and `context_squire_system_prompt_v2.md` now carry inline "Implementation status (runtime v1)" annotations everywhere. Reconciled the two originally-flagged items (system-prompt transport/rewrite rationale; spec §8.3's stale rejection-handling line) plus 12 more divergences found by a systematic section-by-section diff. See its decisions.md for the full 14-item inventory — 7 resolved as doc updates describing intentional runtime behavior, 5 flagged as genuine unimplemented gaps [ask_user response-field loop (known, sa-5), plus 4 newly-discovered gaps: graph traversal/`num_hops`, `accumulated_hits`/`effective_priority` scoring, user-input auto-chunking, raw-partition audit storage — none of the 4 new ones claimed by any todo.json yet].)
7. `retrieval-fidelity` — graph traversal + hit-count/effective-priority scoring (✅ completed 2026-07-02 — the two gaps `protocol-doc-sync` flagged as the spec's explicit core differentiators from "a RAG wrapper" (§7.3) are now real. `explore()`'s `num_hops` performs a real undirected BFS over `squire_relationships` in both `InMemorySquireStore` and `LanceDbSquireStore`, returning graph-connected tokens with `hop_distance`/`via_token_id` provenance and a hop-decayed score. `accumulated_hits`/`effective_priority` (spec §3.3's exact formula) are real fields/computations in both stores, incremented at `upsert_token`/`preserved_tokens`/`token_to_detail` and used as a near-tie ranking tiebreaker. See its decisions.md for the scoring-event-wiring scope decision — 2 of the spec's 4 listed hit-count events are wired directly, 2 are covered by a narrower proxy, tracked as a small deferred follow-up (`rf-13`), not silently treated as complete.)
8. `stream-sigil-fix` — live-stream sigil-leak fix (✅ completed 2026-07-02 — closes `squire-adapter/todo.json` sa-4. Squire mode's raw, sigil-laden protocol JSON output is no longer forwarded to the live `stream-chunk` UI channel during generation; `streaming_cmd.rs` gained a pure `should_stream_live_chunks(context_mode)` helper gating three emission sites — the main per-token chunk handler plus two tool-loop-path progress emissions — to Legacy mode only. Squire mode's finalized, sigil-expanded content still appears correctly at turn close via the unchanged `finalize_turn`/`stream-done` path; Legacy mode's live-streaming behavior is byte-for-byte unchanged. See its decisions.md for why the simplest of three considered approaches — full live-suppression, matching sa-4's own prescribed fix — was chosen over incremental sigil-masking or incremental deterministic expansion.)
9. `ask-user-loop` — Squire response-field AskUser loop wiring (✅ completed 2026-07-03 — closes `squire-adapter/todo.json` sa-5. `SquireContextAdapter::finalize_turn` now returns a new `TurnOutcome::AskUser { question }` variant instead of hard-erroring when the model populates `ask_user`; `streaming_cmd.rs` pauses the turn, emits a new `stream-ask-user-pending` IPC event, and awaits a paired `oneshot::Receiver<String>` via a new `PendingAskUserQuestions` registry — structurally identical to the pre-existing `PendingApprovals` tool-approval pattern — resolved by a new `answer_ask_user_question` Tauri command once the frontend submits an answer through a new inline question/answer UI in `chat-panel.tsx` (mirroring the existing `pendingApprovals` idiom, no modal). On answer, the model's question-bearing response plus a `{"user_answer": "..."}` envelope are appended to the turn's messages and generation resumes via the same `continue` mechanism the `Retry` outcome already uses. Abandonment is handled by reusing three pre-existing mechanisms — task abort, app-close state loss, clean error on a stale answer — rather than a new timeout system. First node in the epic to achieve real end-to-end manual verification: both a new headless real-model Rust harness (`src-tauri/examples/ask_user_e2e.rs`) and a new real WDIO+tauri-driver e2e spec (`e2e/specs/ask-user-loop.test.ts`, 2/2 runs passing) confirmed the full pause/surface/collect/resume loop against a real model and a real running app. See its decisions.md for the full design and verification methodology.)
10. `tool-token-ingestion` — real tool-token ingestion (✅ completed 2026-07-03 — closes `squire-storage/todo.json` ss-9. A new backend-agnostic free function, `agent::squire::ingest_tool_registry(registry, store)`, upserts one `tool`-typed `SquireStore` token per entry in the app's real `ToolRegistry` (local built-ins + MCP-discovered tools) via the trait's existing `upsert_token` method — no new `SquireStore` trait surface was needed. Wired into `streaming_cmd.rs`'s existing per-turn `ToolRegistry` construction (the one real trigger point that already exists — the registry is rebuilt fresh every turn, both context modes), so the store side is no longer permanently empty for `SquireInvokeTool`'s pre-existing registry-primary/store-fallback dispatch (`squire-storage/decisions.md`'s design) to actually resolve something. Token ids are the registry's own tool names verbatim (unprefixed, matching what `invoke()`/`SquireInvokeTool` already key on); `full_desc` is a `{name, description, input_schema}` JSON envelope matching both the spec's "Standard MCP tool schema" format and `SquireTokenToDetailTool`'s pre-existing full-detail shape. Re-ingestion updates existing rows via `upsert_token`'s existing semantics rather than duplicating them; stale tokens for since-removed tools are deliberately left as informational history rather than actively cleaned up (no new enumerate/delete trait surface was added). Verified via 13 new unit tests across both `InMemorySquireStore` and `LanceDbSquireStore`, plus a new headless integration harness (`src-tauri/examples/tool_token_ingestion_e2e.rs`) that ran the real production call chain end to end against a real LanceDB backend — no WDIO/GUI e2e spec was built, since this node adds no new user-facing surface. See its decisions.md for the full design, the token-id/content/removal decisions, and the verification-methodology rationale.)
11. `session-creation-ux` — frontend UI to create a Squire-mode session (✅ completed 2026-07-03 — closes the "newly observed gap" flagged at the end of `tool-token-ingestion`'s session: no button/toggle in the chat UI could create a Squire-mode session, so `context_mode` was only reachable via a direct `create_conversation` IPC call. A `Switch` (the pre-existing, previously-unused-here `components/ui/switch.tsx`) now sits next to `ConversationSidebar`'s existing "+ New session" button, defaulting off (Legacy, matching pre-existing implicit behavior exactly) with Squire as an explicit opt-in read once at click time and forwarded through `chat-store.ts`'s `createNewConversation(contextMode?)` to the already-existing `createConversation(title, contextMode)` IPC wrapper — no backend contract changes were needed for creation itself, only a real caller. A small "Squire" badge (title-attributed, shown only for `contextMode === 'squire'` rows — no badge for the default/expected Legacy case) was added to each sidebar row, which required extending `SessionSummary` end-to-end (Rust struct + `list_sessions` SQL/mapping + frontend type/mapper) since it previously carried no mode information at all. No "change mode later" capability was added or considered, preserving `session-mode`'s immutable-by-construction guarantee exactly. Verified via 2 new backend unit tests (`sqlite_store.rs`, previously untested — `list_sessions` reporting per-session mode correctly) plus 5 new frontend unit tests (3 sidebar, 2 store); `cargo test --lib` 173/173; `npm test -- --run` 82/84 (same 2 pre-existing failures). **Real end-to-end verification performed and passed** via a new WDIO+tauri-driver spec (`e2e/specs/session-creation-ux.test.ts`, 2 cases, each run twice, all passing) that drove the actual UI toggle end to end — the first node in this epic to verify session creation itself through the real UI rather than an IPC reach-in — and, notably, both runs got the free-tier test model to fully close a real turn with real assistant content (a first for this epic's real-model verification; every prior ask_user-adjacent run saw a small model re-ask rather than close). See its decisions.md for the UI-placement, default-behavior, and visual-indicator design reasoning.)

With `retrieval-fidelity`, `stream-sigil-fix`, `ask-user-loop`, `tool-token-ingestion`, and `session-creation-ux` complete, all three features `protocol-doc-sync` flagged as most load-bearing or longest-standing are closed, both `squire-adapter`-flagged gaps (sa-4, sa-5) are resolved, `squire-storage`'s own flagged follow-up (ss-9) is resolved, and the Squire pipeline is now reachable through ordinary UI interaction end to end, not only via direct IPC calls in test code. See `handoff.md` for the current epic-closeout judgment call.

12. `user-input-chunking` — user-input auto-chunking into `USR_TN_NNN` tokens (✅ completed 2026-07-03 — closes the "user-input auto-chunking" gap `protocol-doc-sync` flagged as item 11, unclaimed ever since. `SquireContextAdapter::build_turn_input` now auto-chunks the latest user message — paragraph-then-sentence "natural language structure" splitting, a documented judgment call given the spec's three-word phrase and no chunk-size constant in its own configuration table — into one or more `USR_T{turn}_{NNN}`-id `system_referential`-typed `SquireStore` tokens (`agent::squire::ingest_user_input_chunks`, backend-agnostic via the existing `upsert_token`, no new trait method), created before the turn's bootstrap vector search so they are immediately discoverable within the same turn (spec §9.1's numbered step order). Resolved three further spec ambiguities the task flagged as needing judgment: chunking triggers unconditionally on every user message (no size threshold — nothing in the spec suggests one); the `USR_T{turn}_{NNN}` numbering sequence resets to `001` at the start of each turn rather than running as a session-lifetime monotonic counter (the only reading consistent with spec §3.1's own `USR_T2_001` worked example encoding a turn number distinct from the sequence); chunk tokens are referenced afterward purely as ordinary store tokens the AI can `explore()`/`token_to_detail()`/relationship-link like any other token — no sigil-parsing of the user's own raw input was added, since `§!`/`§^` sigils are exclusively an AI-output convention per spec §5.1/§5.2. Per spec §4.3's explicit instruction, no relationships are auto-generated for chunk tokens. Verified via 20 new unit tests (17 in `squire.rs` against `InMemorySquireStore`, 3 in `squire_lancedb.rs` against the real `LanceDbSquireStore`) plus a new headless integration harness (`src-tauri/examples/user_input_chunking_e2e.rs`) that ran the real `build_turn_input` call chain end to end against a real LanceDB backend, confirming both the same-turn bootstrap-discoverability ordering requirement and the per-turn numbering-reset behavior — no WDIO/GUI e2e spec was built, since this node adds no new user-facing surface (confirmed via a repo-wide frontend grep), the same judgment `tool-token-ingestion` made for its own similarly backend-only change. See its decisions.md for the full four-judgment-call design writeup and a newly flagged, not-claimed follow-up: the pre-existing `"memory"` `resource_type` alias doesn't yet include `system_referential` in its expansion.)

With `user-input-chunking` also complete, the residual backlog was: raw-partition audit-log storage, `retrieval-fidelity/todo.json` rf-13 (fuller hit-count-event fidelity), the endpoint-carrying `TokenDetail`/`invoke()` extension `tool-token-ingestion` deliberately left out of scope, the `"memory"`-alias/`system_referential` follow-up this node flagged, and the two small optional UX follow-ups `session-creation-ux` surfaced (toggle persistence, active-conversation mode indicator).

13. `raw-partition-storage` — raw-partition audit-log storage (✅ completed 2026-07-03 — closes `protocol-doc-sync/decisions.md`'s item 12, the last of that session's four newly-discovered gaps to be resolved (graph traversal/hit-count scoring closed by `retrieval-fidelity`; user-input auto-chunking by `user-input-chunking`). Resolved the central ambiguity the task flagged before implementing: the spec's raw partition is **not** a verbatim, full-response audit trail redundant with the ordinary chat-history table — it is specifically the portion of a compliant turn's AI output that fell **outside** every closed `§^...§^` span, i.e. content the model produced but chose not to promote into a structured, addressable memory token. This reading follows directly from §4.1's own two-partition contrast ("Raw partition — auto-stored AI output that **was not explicitly marked with §^**"), §4.3's explicit conditional framing ("if the AI does not mark a span, it is stored **only** in the raw partition"), and §9.4's numbered turn-close sequence treating "parse §^ spans" (step 2, to the structured partition) and "store raw response" (step 4, to the raw partition) as two destinations for one shared source text rather than two independent full copies. Also resolved: no read-back mechanism exists anywhere in the spec for this partition — "Explore() does not search this partition by default" is read as descriptive of `explore()`'s own scope, not a promise of some other unspecified read path, and no fourth tool/mechanism is described anywhere in the protocol — so this is a write-only, operator/debugging-facing audit log, matching the existing `squire_compliance_failures` table's "never queried for runtime decisions, only inspected for diagnostics" precedent exactly. Implementation: a new `SquireStore::record_raw_output(session_id, turn, content)` trait method (both `InMemorySquireStore` and `LanceDbSquireStore`), a new sixth LanceDB table `squire_raw_partition` (session_id/turn/content/timestamp, no embedding column — nothing in this runtime ever vector-searches it), and a new pure helper `unmarked_residual(content) -> String` in `agent::squire` (a close sibling of the pre-existing `strip_span_markers`, reusing the same `split("§^")` traversal shape) that extracts exactly the text outside every closed span. Wired into `SquireContextAdapter::finalize_turn` immediately after the pre-existing `extract_spans` call, on the compliant path only (a rejected response never reaches this point — it already gets a complete structured audit trail via the pre-existing `record_compliance_failure` path instead), skipping the write entirely when the residual is empty (a fully-`§^`-spanned response has nothing left over to archive). Verified via 13 new unit tests (10 in `squire.rs`: 6 pure-function `unmarked_residual` edge-case tests plus 4 `finalize_turn` integration tests against `InMemorySquireStore`; 3 in `squire_lancedb.rs` against the real `LanceDbSquireStore`) plus a new headless integration harness (`src-tauri/examples/raw_partition_storage_e2e.rs`) that ran the real `finalize_turn` call chain end to end against **both** real backends it touches — a real `LanceDbSquireStore` and, new for this node, a real SQLite-backed `ConversationStore` (`state::db::Database`) — confirming a mixed marked/unmarked response persists only the unmarked prose to the new table while the marked span is still separately promoted to a real `squire_tokens` row and the ordinary chat-history table is unaffected; a fully-spanned response and a rejected response both correctly write nothing to the raw partition. No WDIO/GUI e2e spec was built, since this node adds no new user-facing surface (confirmed via a repo-wide frontend grep), the same judgment `tool-token-ingestion`/`user-input-chunking` made for their own similarly backend-only changes. `cargo build`/`cargo build --bins`/`cargo build --examples`: all clean, zero warnings. `cargo test --lib`: **206/206 passing** (193 baseline + 13 new). See its decisions.md for the full textual argument and design writeup.)

With `raw-partition-storage` also complete, every gap `protocol-doc-sync` ever flagged is now resolved. The residual backlog is now: `retrieval-fidelity/todo.json` rf-13 (fuller hit-count-event fidelity), the endpoint-carrying `TokenDetail`/`invoke()` extension `tool-token-ingestion` deliberately left out of scope, the `"memory"`-alias/`system_referential` follow-up `user-input-chunking` flagged, and the two small optional UX follow-ups `session-creation-ux` surfaced (toggle persistence, active-conversation mode indicator). See `handoff.md` for the current epic-closeout judgment call.

14. `session-ux-polish` — the two small UX follow-ups `session-creation-ux` surfaced (✅ completed 2026-07-03 — closes both items in `session-creation-ux/state.md`'s Risks section: (1) the Squire-mode creation toggle (`ConversationSidebar`'s `nextSessionSquireMode`) now persists its last-chosen value via `localStorage` — two new functions, `loadStoredSquireModeDefault`/`saveStoredSquireModeDefault`, added to `chat-store/preferences.ts` mirroring that file's existing provider/model/thinking-level persistence pattern exactly — so a user no longer has to re-flip the toggle after a remount/app restart, while a brand-new/never-toggled install still defaults to Legacy exactly as before (session-creation-ux's core "least disruption" guarantee is unchanged, since this only makes an already-expressed preference sticky, not the first-run default); (2) the active conversation's own chat header (`chat-panel.tsx`'s "Chat" tab) now shows a small "Squire" badge — visually identical to the pre-existing sidebar row badge (same classes, same title copy) — derived via `conversations.find(c => c.id === activeConversationId)?.contextMode` (no new IPC/store surface needed, `SessionSummary.contextMode` was already plumbed end-to-end by `session-creation-ux`), shown only when the open conversation is Squire mode, matching the sidebar badge's own "badge only for the non-default case" convention for consistency. No "change mode later" capability was added or considered, preserving `session-mode`'s immutable-by-construction guarantee exactly. Verified via 5 new frontend unit/component tests (2 in `conversation-sidebar.test.tsx` for persistence, 3 in a new `chat-panel.test.tsx` for the header badge — no prior test file existed for that component) plus real end-to-end verification: extended `e2e/specs/session-creation-ux.test.ts` with a new case that toggles Squire on, forces a real remount via `browser.refresh()`, and confirms the toggle is still checked — while verifying this new case wasn't itself flaky, this session found and fixed a real, pre-existing timing race in an *existing*, unmodified-by-this-node assertion in that same spec file (a non-polling `expect(...).toBe('checked')` immediately after a toggle click, made more likely to manifest by this session's persistence write adding a small amount of synchronous work to the same click handler), replacing it with a polling `browser.waitUntil(...)` matching the idiom already used elsewhere in the file; confirmed clean across 4 consecutive full-suite runs (12 test-case executions, all passing) after the fix, plus one clean re-run of `ask-user-loop.test.ts` confirming no regression. The chat header indicator itself was verified via component tests rather than a dedicated e2e case, judged proportional for a purely presentational, purely-derived UI element with no state transitions of its own. `cargo build`/`cargo test --lib`: unchanged, clean/206/206 (pure frontend node, no backend files touched). `npx tsc --noEmit`: zero new errors (same 7 pre-existing `tools-panel.tsx` errors). `npm test -- --run`: 87/89 (82 baseline + 5 new), same 2 pre-existing failures. See its decisions.md for the full persistence-mechanism, header-indicator-placement, and e2e-flakiness-fix design reasoning.)

With `session-ux-polish` also complete, both items in `session-creation-ux`'s own follow-up list are resolved. The residual backlog was: `retrieval-fidelity/todo.json` rf-13 (fuller hit-count-event fidelity), the endpoint-carrying `TokenDetail`/`invoke()` extension `tool-token-ingestion` deliberately left out of scope, and the `"memory"`-alias/`system_referential` follow-up `user-input-chunking` flagged.

15. `hit-count-fidelity` — fuller hit-count-event fidelity, closing `retrieval-fidelity/todo.json` rf-13 (✅ completed 2026-07-03 — `SquireContextAdapter::finalize_turn` (`src-tauri/src/agent/squire.rs`) now credits an `accumulated_hits` hit, via the pre-existing `record_hit` method (`retrieval-fidelity`; no new `SquireStore` trait surface was needed), for every already-existing token `§!`-referenced in a compliant response's `content` — closing the "...or references in output" disjunct of spec §3.3's event 1 ("Token appears in explore() results that AI acts on") and event 3 ("§! reference found in a chunk loaded into context") for the AI's own response content. `retrieval-fidelity`'s original proxy credited a hit only when the model called `token_to_detail` on a token — the far more common pattern of an AI citing a token directly in its visible output purely from having seen its `short_desc` in a prefetch/preserve/explore list, without ever calling `token_to_detail`, previously earned zero credit; it now does. Guarded against double-crediting: a token both defined in `new_tokens` and cited via `§!` in the same turn is correctly credited exactly once (via `upsert_token`'s pre-existing event-4 "regardless" rule), not twice, since the new crediting loop only fires for ids already present in `finalize_turn`'s pre-existing `known` set — which is computed via `token_exists` *before* the turn's own `new_tokens` are upserted. One narrower residual is deliberately left unwired and explicitly documented, not silently dropped: a `§!` reference nested *inside* a `full_desc` body itself (a chunk citing a different chunk, only surfaced when that body is loaded via `token_to_detail`) is still not scanned for embedded references — closing this fully would require a genuine context-composition audit pass scanning every piece of content that ever enters context, the same disproportionate-infrastructure concern `retrieval-fidelity/decisions.md` originally flagged for the broader gap this node substantially narrows; a future node could close it by extending `SquireTokenToDetailTool::execute` to scan its returned `full_desc`/`short_desc` via the same `extract_inline_refs` helper reused here. No `SquireStore` trait changes, no signature changes to any existing method — the entire change is one new loop in `finalize_turn` calling an already-existing, already-tested primitive. Verified via 4 new unit tests (3 in `squire.rs` — citing without `token_to_detail` earns a hit, same-turn define-and-cite earns exactly one hit not two, repeated citations of one token in one response still credit exactly one hit — against real `finalize_turn` integration paths through `InMemorySquireStore`; 1 in `squire_lancedb.rs` confirming the real `LanceDbSquireStore`'s `record_hit`/`upsert_token` primitives compose identically). `cargo build`/`cargo build --bins`/`cargo build --examples`: all clean, zero warnings. `cargo test --lib`: **210/210 passing** (206 baseline + 4 new). No WDIO/GUI spec or new headless example harness was needed — confirmed via a repo-wide frontend grep (zero hits for `accumulated_hits`/`hit-count`/`record_hit`) that this remains pure backend scoring logic with no user-facing surface, and the change itself needed no new cross-process/storage data flow beyond what `retrieval-fidelity`'s own tests already exercise. See its decisions.md for the full operationalization, the double-count-guard reasoning, and the deliberately-deferred nested-citation tradeoff.)

With `hit-count-fidelity` also complete, rf-13 is resolved. The residual backlog is now: the endpoint-carrying `TokenDetail`/`invoke()` extension `tool-token-ingestion` deliberately left out of scope, and the `"memory"`-alias/`system_referential` follow-up `user-input-chunking` flagged. See `handoff.md` for the current epic-closeout judgment call.

16. `token-detail-endpoint` — the endpoint-carrying `TokenDetail`/`invoke()` extension `tool-token-ingestion/env.md` deliberately left out of scope, originally described in `squire-storage/decisions.md` as a second, separate "full cutover" piece (✅ completed 2026-07-03 — closes the gap. Before implementing, this node made a clear-eyed proportionality assessment (`decisions.md`'s "Proportionality assessment" section), since the task's own framing worried real dispatch might require disproportionate new MCP session/connection lifecycle infrastructure "outside the normal per-turn flow." Reading the actual MCP dispatch code (`src-tauri/src/mcp/mod.rs`) in full overturned that worry: `crate::mcp::call_tool` is **already** a stateless, one-off-per-call operation — every MCP tool call, live or not, already spins up a fresh `StdioMcpClient`, connects, calls, and disconnects; there is no persistent MCP session anywhere in this codebase to begin with. Combined with `McpServerConfig` already being a small, plain, `Clone`/`Serialize` struct, this made real dispatch tractable and proportionate rather than a disproportionate new feature — so this node implemented **full real dispatch**, not a scoped-down diagnostic. Also confirmed (`ToolRegistry::new()`'s unconditional local-builtin registration) that this gap can only ever be meaningful for MCP-sourced tools, never local built-ins. Implementation: `TokenDetail`/`NewTokenSpec` gained a new `endpoint: Option<ToolEndpoint>` field (`ToolEndpoint::Mcp { server: McpServerConfig, remote_name: String }`, the only variant); both `InMemorySquireStore` and `LanceDbSquireStore` persist/return it (a new nullable, JSON-serialized `endpoint` column in the LanceDB tokens table, following `retrieval-fidelity`'s established "nullable column, no destructive migration" precedent); `ingest_tool_registry` gained a new `endpoints: &HashMap<String, ToolEndpoint>` side-channel parameter, purely additive (every pre-existing call site passes an empty map, unchanged behavior), populated only in `streaming_cmd.rs`'s MCP-discovery loop — the one place a tool's MCP origin is still known before `ToolDefinition` erases it; `SquireInvokeTool`'s store-fallback branch now genuinely dispatches via `crate::mcp::call_tool` (the same primitive `McpProxyTool::execute` already uses for live tools) when a stored MCP endpoint is present, instead of only returning a diagnostic — the original diagnostic is preserved unchanged for tokens with no endpoint (local-builtin tokens, or MCP tokens ingested before this node, which self-heal on the next turn's re-ingestion). A security constraint was identified and verified: `McpServerConfig` can carry secrets in `env`/`headers`, so `endpoint` must never reach the model — confirmed `SquireTokenToDetailTool::execute` never re-serializes the whole `TokenDetail` struct, with a new regression test proving no leak even for a token with a real endpoint recorded. An incidental, real pre-existing-test-breaking bug was found and fixed during implementation: `LanceDbSquireStore::record_hit` builds its own `RecordBatch` independently of `upsert_token`'s and hadn't been updated for the new column, silently no-op'ing (7 pre-existing tests failed) until fixed. Verified via 11 new unit tests (7 in `squire.rs` against `InMemorySquireStore`: serde round-trip, upsert persists/returns endpoint, upsert-without-endpoint preserves a prior one, ingestion populates endpoint only for MCP-sourced definitions plus an empty-map regression guard, real dispatch to a stored-but-not-live endpoint via a deliberately unreachable fake command proving the real `call_tool` path is exercised rather than mocked, and the `SquireTokenToDetailTool` no-leak security test; 4 in `squire_lancedb.rs` against the real `LanceDbSquireStore` mirroring the same coverage plus the `record_hit`-preserves-endpoint regression test for the bug this node found). `cargo build`/`cargo build --bins`/`cargo build --examples`: all clean, zero warnings. `cargo test --lib`: **221/221 passing** (210 baseline + 11 new). No WDIO/GUI spec or new headless example harness was needed — confirmed via a repo-wide frontend grep (zero hits for `ToolEndpoint`) that this remains pure backend dispatch logic with no user-facing surface. See its decisions.md for the full proportionality assessment, the two key findings that overturned the task's initial disproportionate-infrastructure worry, the `ToolEndpoint`/`TokenDetail` shape design, and the security-constraint reasoning.)

With `token-detail-endpoint` also complete, this gap is resolved via full implementation (not a scope-down). The residual backlog was: the `"memory"`-alias/`system_referential` follow-up `user-input-chunking` flagged, and the nested-`§!`-citation residual `hit-count-fidelity` flagged.

17. `memory-alias-fix` — the `"memory"`-alias/`system_referential` gap `user-input-chunking` flagged (✅ completed 2026-07-03 — the `resource_type="memory"` convenience alias in `explore()`'s `type_matches` closure, duplicated in both `InMemorySquireStore` (`squire.rs`) and `LanceDbSquireStore` (`squire_lancedb.rs`), expanded to `concept`/`referential` token types only — a list that predated `system_referential` (the type `user-input-chunking` gave to `USR_T{turn}_{NNN}` chunk tokens) and was never updated when that type was added. A model using the "memory" shortcut could not discover the AI's own chunked user-input tokens, even though `resource_type="system_referential"` (exact) and `resource_type="all"` both already found them correctly — this was purely a gap in one convenience alias's expansion list, not a broader discoverability defect. Fixed identically in both files by adding `t == "system_referential"` to the `"memory"` branch. Verified via 2 new unit tests (one per backend) confirming the alias now surfaces a `system_referential` token created via `ingest_user_input_chunks`. `cargo build`: clean. `cargo test --lib`: **223/223 passing** (221 baseline + 2 new). Per direct user instruction, the other remaining backlog item — the nested-`§!`-citation residual `hit-count-fidelity` flagged — was explicitly left unaddressed rather than closed ("ignoring nesting feels more right"), treated as a final, deliberate product decision rather than a deferral. See its decisions.md for the fix and the explicit non-fix reasoning.)

With `memory-alias-fix` also complete and the nested-citation residual deliberately left as a permanent, documented simplification per direct user instruction, the Squire epic's residual backlog is empty of anything still intended to be closed. See `handoff.md` for the current epic-closeout status.

