# Prompt — Phase 7: Post-MVP

Deferred until the MVP is shipped and real usage patterns emerge.

## Future work
7.1 Plugin/extension system — evaluate Wasmtime for sandboxed plugins
7.2 MCP-like protocol — redefine how MCP works as a plugin layer, not baked into the core
7.3 Anything that emerges from real user feedback
